1.工厂方法模式：

定义一个用于创建对象的接口，让子类决定实例化哪一个类。工厂方法使一个类的实例化延迟到了子类中。

注：

简单工厂模式的最大优点在于工厂类中包含了必要的逻辑判断，根据客户端的选择动态实例化相关的类，对于客户端来说，去除了具体产品的依赖。

用简单的话说简单工厂和工厂方法的区别：（个人感觉简单工厂更加地实用）

简单工厂把逻辑判断放在工厂类中，工厂方法则是用多态来动态地实例化需要的实例。