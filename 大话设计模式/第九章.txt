1.浅拷贝和深拷贝：

浅拷贝：只拷贝顶层元素，下级引用指向同一个元素。如：Java中的super.clone()方法。
深拷贝：完整拷贝所有元素，下级引用创建新的元素来指向新的对象。深拷贝时直接用构造方法创建新的对象。

2.原型模式：

用原型实例指定创建对象的种类，并且通过拷贝这些原型创建新的对象。

从一个对象再创建另外一个可定制的对象，而且不需要知道任何创建的细节。

3.原型模式的优势：

一般在初始化信息不发生变化的情况下，克隆是最好的办法，既隐藏了对象的细节，又对性能大大提高。

原型模式等于不用重新初始化对象，而是动态地获得对象的运行时状态。
